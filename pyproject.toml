[project]
name = "mcp-server-demo"
version = "0.1.0"
description = "MCP 计算器示例项目"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.6.0",
    "fastmcp>=0.1.0",
    "pydantic>=2.0.0",
    "uvicorn>=0.15.0",
    "typer>=0.15.2",
]
